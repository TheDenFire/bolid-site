meta:
  environment: jvm
  toolchain:
    name: maven
    version: "17"
build:
  image: null
  args: spring-boot:repackage
  artifacts:
    tg-bot/target/*.jar: /
run:
  image: null
  jarName: bolid-site.jar
  persistenceMount: /data
  containerPort: "8080"
  servicePort: "80"
